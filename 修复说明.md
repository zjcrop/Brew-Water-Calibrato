# 本次修复内容

- 增加 `android-actions/setup-android@v3`，确保 `sdkmanager` 存在并正确进入 PATH。
- 将 GitHub 官方 Actions 升级到 Node.js 24 运行时版本：
  - `actions/checkout@v6`
  - `actions/setup-node@v6`
  - `actions/setup-java@v5`
  - `actions/upload-artifact@v7`
- 使用 `gradle/actions/setup-gradle@v6` 配置 Gradle。
- 构建时改为 `bash ./gradlew`，并先清理潜在 CRLF 行尾，避免 shell 返回 127。
- 增加 `command -v sdkmanager`、`sdkmanager --version` 和 Gradle 版本诊断。
